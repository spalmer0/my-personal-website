import { Link } from 'gatsby';
import React from 'react';

const Contact = (props) => (
    <div>
        <h1>Contact Me</h1>
        <Link to="/about">About Me</Link>
    </div>
);

export default Contact;